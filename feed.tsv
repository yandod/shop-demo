---
layout: null
---
id	title	description	google_product_category	link	image_link	condition	availability	price	brand	product_type
{% for product in site.data.products limit:10 %}{{ product.id }}	{{ product.title }}	{{ product.description }}	{{ product.google_product_category }}	{{ product.link }}	{{ product.image_link }}	{{ product.condition }}	{{ product.availability }}	{{ product.price }}	{{ product.brand }}	{{ product.product_type }}
{%endfor %}
